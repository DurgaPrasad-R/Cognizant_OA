CREATE TABLE RES AS 
   (SELECT CO.*,OD.*
    FROM CUSTOMER_ORDER CO
    JOIN ORDER_DELIVERY OD
    ON OD.ORDER_ID = CO.ORDER_ID);

SELECT CA.FIRST_NAME AS 'First_Name',
       RES.STATUS AS 'Status'
FROM CUSTOMER_ADDRESS CA
JOIN RES
ON CA.CUSTOMER_ID = RES.CUSTOMER_ID;