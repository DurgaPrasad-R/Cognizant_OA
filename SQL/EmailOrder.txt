CREATE TABLE RES
AS
(SELECT OD.STATUS,CO.CUSTOMER_ID
 FROM CUSTOMER_ORDER CO
 JOIN ORDER_DELIVERY OD
 ON CO.ORDER_ID = OD.ORDER_ID);


SELECT CA.EMAIL AS EMAIL_CUST_ID
FROM CUSTOMER_ADDRESS CA
JOIN RES R
ON CA.CUSTOMER_ID = R.CUSTOMER_ID;